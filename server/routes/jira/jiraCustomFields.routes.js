import { Router } from 'express';
import * as JiraController from '../../controllers/jira';
const router = new Router();

// router.route('/').post(JiraController.CustomFields.upsertCustomFields);
module.exports = router;
